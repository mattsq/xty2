# Tier 2 — published-number reproduction

Empty until P12 (`docs/PLAN.md`). Tests here are driven by each recipe card's
§6 `reproduction` block and run nightly, not on PRs. The nightly workflow goes
green on an empty directory by design — see `tests/conftest.py`.
